# Copy to .env (gitignored). Values are referenced from config YAML as ${VAR}.
HA_TOKEN=paste-your-long-lived-access-token-here
# HA_URL is only used by `pnpm ha:explore`; the server reads url from config YAML.
# HA_URL=http://homeassistant.local:8123
